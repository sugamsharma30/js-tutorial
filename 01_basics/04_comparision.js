console.log("2"> 1);
// it will give true as "2" string will automatically converted to number 2 

      //**NULL**
      console.log(null>0);// false
      console.log(null==0)// flase 
      console.log(null>=0);// true as it will convert null to 0 so this operation is true 

        //**undefined**
        console.log(undefined==0);
        console.log(undefined>0);
        console.log(undefined<0);
        
         //***STRICT CHECK***
         // === it will not only check the value and also the datatype
         console.log("2"=== 2);// it wil also check datatype and they are not same so output wil be false.
         
         

      
